<div class="page-header"><h3>Form &amp; Tab Related Functionality</h3><hr class="notop"></div><ol><li><div id="form_tab_func"><h4>How to open more then one new tabs in browser using javascript?   </h4>To open no of new tabs in browser , We have to dynamically create forms on fly on any particular event like page load, on click etc. <br/><strong>Download Source File :</strong> <a href="download/form_tab_func_ex.rar" >SRC Files</a><h6>File Name :script.js </h6><pre class="prettyprint linenums"><code class="language-js">//helper function to create the formfunction getNewSubmitForm(val){ var submitForm = document.createElement("FORM"); document.body.appendChild(submitForm); submitForm.method  = "POST"; submitForm.target  ="_BLANK"; submitForm.name     ="frm"; submitForm.id        = val; return submitForm;}</code>	</pre>Above function create form element and its set its attributes. <pre class="prettyprint linenums"><code class="language-js">//helper function to add elements to the formfunction createNewFormElement(inputForm, elementName, elementValue){    //Create an input type dynamically.    var element = document.createElement("input");    //Assign different attributes to the element.    element.setAttribute("type", "hidden");    element.setAttribute("value", elementValue);    element.setAttribute("name", elementName);    inputForm.appendChild(element);    return element;}</code>	</pre>This function set the hidden fields and value of form elements. <pre class="prettyprint linenums"><code class="language-js">//function that creates the form, adds some elements//and then submits itfunction createFormAndSubmit(val){     var submitForm = getNewSubmitForm(val);     createNewFormElement(submitForm, "act", "checkurl");     createNewFormElement(submitForm, "websiteId", val);     submitForm.action= "test-url.php";     submitForm.submit();     return false;}</code>	</pre>Above function create form element and submit form. <pre class="prettyprint linenums"><code class="language-js">function start(start,end){     for(var i=start;i<=end;i++){         alert(i);         createFormAndSubmit(i);     }}</code>	</pre><strong>How to call main function : </strong><pre class="prettyprint linenums"><code class="language-js">	onclick="start()"</code>	</pre></div></li></ol>